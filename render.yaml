services:
  - type: web
    name: songbox-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: DATABASE_PROVIDER
        value: supabase
      - key: PYTHON_VERSION
        value: 3.11.10
